os:
  - linux
  - osx
addons:
    sources:
      - sourceline: ppa:plt/racket
    apt:
        packages:
          - racket
before_install:
  - |
    if [ "$TRAVIS_OS_NAME" == osx ]
    then
        brew install minimal-racket
    fi
before_script:
  - racket -v
script:
  - raco test tests
